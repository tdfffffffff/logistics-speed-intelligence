# Core analysis
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # required by pandas for Parquet I/O
duckdb>=0.10           # SQL engine backing the natural-language querying agent

# Visualisation
matplotlib>=3.8

# Language model providers
# All are optional: the notebook replays cached responses when no key is present.
google-genai>=1.0      # Gemini
groq>=0.11             # GPT-OSS and Qwen open-weight models
openai>=1.40           # OpenAI direct access
transformers>=4.44     # local on-premise model (Section 13.6)
torch>=2.2             # required by transformers
accelerate>=0.30       # required by transformers for model loading

# Prototype and reporting
gradio>=4.44           # prototype interface
python-docx>=1.1       # manager brief generation
docx2pdf>=0.1.8        # optional PDF export; requires Microsoft Word or LibreOffice
python-dotenv>=1.0     # loads API keys from .env
tabulate>=0.9          # required by pandas .to_markdown() in the querying agent
